name: Carga eleicoes2026 (TSE + Wikipedia -> Databricks -> dbt)

on:
  schedule:
    # 09:00 UTC = 06:00 em Brasília, todos os dias
    # 08:17 UTC = 05:17 em Brasilia. Minuto quebrado de proposito: o GitHub atrasa muito
    # os agendamentos na hora cheia (o "0 9" chegava a rodar 4h depois).
    - cron: "17 8 * * *"
  workflow_dispatch:
    inputs:
      rodar_tse:
        description: "Carregar fontes do TSE"
        type: boolean
        default: true
      onde_tse:
        description: "Onde baixar o TSE: github (padrão) ou local (runner no computador)"
        type: choice
        options: [github, local]
        default: github
      rodar_wikipedia:
        description: "Carregar Wikipedia (runner do GitHub)"
        type: boolean
        default: true
      rodar_dbt:
        description: "Disparar o job do dbt após a carga"
        type: boolean
        default: true

concurrency:
  group: carga-eleicoes2026
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  # Carga do TSE. O CDN (cdn.tse.jus.br) tem anti-bot: a carga usa curl_cffi (assinatura Chrome).
  # Padrão: runner do GitHub. Alternativa: runner self-hosted no computador local, escolhida pela
  # variável de repositório/environment RUNNER_TSE=local (agendado) ou pelo input onde_tse=local.
  carga_tse:
    name: Carga TSE (runner GitHub)
    if: ${{ (github.event_name == 'schedule' && vars.RUNNER_TSE != 'local') || (github.event.inputs.rodar_tse == 'true' && github.event.inputs.onde_tse != 'local') }}
    environment: producao
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: ingestion/requirements.txt

      - name: Instalar dependências
        run: pip install -r ingestion/requirements.txt

      - name: Carregar TSE
        env:
          DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
          DATABRICKS_WAREHOUSE_ID: ${{ vars.DATABRICKS_WAREHOUSE_ID }}
          DATABRICKS_CATALOG: ${{ vars.DATABRICKS_CATALOG || 'workspace' }}
          DATABRICKS_SCHEMA: ${{ vars.DATABRICKS_SCHEMA || 'raw_eleicoes' }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          FONTES: pesquisa_eleitoral_2026,totalizacao_presidente_2022
        run: python ingestion/carga_raw.py

  carga_tse_local:
    name: Carga TSE (runner local)
    if: ${{ (github.event_name == 'schedule' && vars.RUNNER_TSE == 'local') || (github.event.inputs.rodar_tse == 'true' && github.event.inputs.onde_tse == 'local') }}
    environment: producao
    runs-on: [self-hosted, Windows]
    timeout-minutes: 45
    defaults:
      run:
        shell: powershell
    steps:
      - uses: actions/checkout@v4

      - name: Preparar Python (venv)
        run: |
          $ErrorActionPreference = "Stop"
          $candidatos = @(
            (Get-Command py -ErrorAction SilentlyContinue).Source,
            (Get-Command python -ErrorAction SilentlyContinue).Source,
            "$env:USERPROFILE\anaconda3\python.exe",
            "$env:USERPROFILE\miniconda3\python.exe",
            "$env:ProgramData\anaconda3\python.exe"
          ) | Where-Object { $_ -and (Test-Path $_) -and ($_ -notlike "*WindowsApps*") }
          if (-not $candidatos) { throw "Python não encontrado no computador do runner" }
          $py = $candidatos[0]
          Write-Host "Python base: $py"
          & $py -m venv .venv
          if ($LASTEXITCODE -ne 0) { throw "falha ao criar venv" }
          & .\.venv\Scripts\python.exe -m pip install --upgrade pip --quiet
          & .\.venv\Scripts\python.exe -m pip install -r ingestion\requirements.txt --quiet
          if ($LASTEXITCODE -ne 0) { throw "falha ao instalar dependências" }

      - name: Carregar TSE
        env:
          DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
          DATABRICKS_WAREHOUSE_ID: ${{ vars.DATABRICKS_WAREHOUSE_ID }}
          DATABRICKS_CATALOG: ${{ vars.DATABRICKS_CATALOG || 'workspace' }}
          DATABRICKS_SCHEMA: ${{ vars.DATABRICKS_SCHEMA || 'raw_eleicoes' }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          FONTES: pesquisa_eleitoral_2026,totalizacao_presidente_2022
          PYTHONIOENCODING: utf-8
          PYTHONUTF8: "1"
        run: |
          & .\.venv\Scripts\python.exe ingestion\carga_raw.py
          exit $LASTEXITCODE

  carga_wikipedia:
    name: Carga Wikipedia (runner GitHub)
    if: ${{ github.event_name == 'schedule' || github.event.inputs.rodar_wikipedia == 'true' }}
    environment: producao
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: ingestion/requirements.txt

      - name: Instalar dependências
        run: pip install -r ingestion/requirements.txt

      - name: Carregar Wikipedia
        env:
          DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
          DATABRICKS_WAREHOUSE_ID: ${{ vars.DATABRICKS_WAREHOUSE_ID }}
          DATABRICKS_CATALOG: ${{ vars.DATABRICKS_CATALOG || 'workspace' }}
          DATABRICKS_SCHEMA: ${{ vars.DATABRICKS_SCHEMA || 'raw_eleicoes' }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          FONTES: intencao_voto_2026
        run: python ingestion/carga_raw.py

  # Transformacao. Roda dbt Core contra o mesmo warehouse da carga, com o profile em ci/profiles.yml.
  # Nao depende de job configurado no dbt Platform (a UI do dbt continua servindo para explorar/editar).
  dbt:
    name: dbt build (Databricks)
    needs: [carga_tse, carga_tse_local, carga_wikipedia]
    # roda se nenhuma carga falhou (cargas desmarcadas contam como "skipped")
    if: ${{ !cancelled() && !contains(needs.*.result, 'failure') && (github.event_name == 'schedule' || github.event.inputs.rodar_dbt == 'true') }}
    environment: producao
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      DBT_PROFILES_DIR: ci
      DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
      DATABRICKS_HTTP_PATH: /sql/1.0/warehouses/${{ vars.DATABRICKS_WAREHOUSE_ID }}
      DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
      DATABRICKS_CATALOG: ${{ vars.DATABRICKS_CATALOG || 'workspace' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dbt
        # 1.12: versao em que o projeto foi validado (sintaxe `arguments:` nos testes exige >= 1.10)
        run: pip install "dbt-core>=1.12,<1.13" "dbt-databricks>=1.12,<1.13"

      - name: dbt deps
        run: dbt deps

      - name: dbt debug
        run: dbt debug --target prod

      # build = seeds + snapshots + models + testes, na ordem do DAG
      - name: dbt build
        run: dbt build --target prod

      - name: Guardar artefatos (manifest, run_results)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dbt-target
          path: target/
          if-no-files-found: ignore

  # Depois do dbt, regenera o painel público (docs/index.html) e publica no GitHub Pages.
  # Roda também quando o dbt é pulado no disparo manual; não roda se algo falhou.
  publicar:
    name: Publicar painel
    needs: [dbt]
    if: ${{ !failure() && !cancelled() }}
    permissions:
      contents: write
    uses: ./.github/workflows/publicar_site.yml
    secrets: inherit
